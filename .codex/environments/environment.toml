# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "open-gikai"

[setup]
script = """

if [ ! -f .env ]; then
  main_repo_root="$(cd "$(git rev-parse --git-common-dir)/.." && pwd)"
  source_env="$main_repo_root/.env.local"

  if [ -f "$source_env" ]; then
    cp "$source_env" .env.local
  else
    echo "skip: source .env not found at $source_env"
  fi
fi
'''

eval "$(mise activate zsh --shims)"
mise trust
mise up

bun install
"""
