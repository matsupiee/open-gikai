name: Test API

on:
  pull_request:
    branches: [main]
    paths:
      - "packages/api/**"
      - "packages/db-minutes/**"
      - "packages/db-auth/**"
      - "packages/auth/**"
      - "package.json"
      - "bun.lock"
      - "tsconfig.json"
      - "turbo.json"

concurrency:
  group: test-api-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-api:
    runs-on: ubuntu-latest
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.10"

      - run: bun install --frozen-lockfile

      - name: Run API tests
        run: bun run --cwd packages/api vitest run
